# This is the container for the backend

# This is the image that we are going to use to create our image
FROM golang:1.16-alpine3.13

# Establishing the working directory on the container
WORKDIR /go/src/app

# Copying files of server folder to our container on the working directory
COPY . .

# Exposing PORT 4001 for the incoming requests
EXPOSE 4001 
ENV PORT=4001

# Building our go project
RUN go build 

# Executing the binary file generated by the previous step
CMD [ "./vuego-demoapp" ]